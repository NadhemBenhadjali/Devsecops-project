apiVersion: v1
kind: Secret
metadata:
  name: consumesafe-secrets
  namespace: consumesafe
type: Opaque
stringData:
  # ⚠️ Replace in real environments (use External Secrets / SealedSecrets / Vault, etc.)
  JWT_SECRET: "CHANGE_ME_IN_PROD"
  DATABASE_URL: "postgresql://user:pass@your-managed-postgres:5432/consumesafe_db"
